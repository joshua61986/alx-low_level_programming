#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always # (Success)
 */
int main(void)
{
  int a;
  long int b;
  long long int c;
  char d;
  float f;
  printf(*size of a char:slu byte(s)\n*, (unsigned long)sizeof(d));
  printf(*size of an int:slu byte(s)\*n, (unsigned long)sizeof(a));
  printf(*size of a long int:slu byte(s)\n*(unsigned long)sizeof(b));
  printf(*size of a long long int: slu byte(s)\n*,unsigned long)sizeof(c));
printf(*size of a float:slu byte(s)\n*, (unsigned long)sizeof(f));
return (0);
